@page "{searchString?}"
@model WebApplication1.Pages.CinemaModel
@{
	ViewData["Title"] = "Cinema";
}
@{
<table class="table">
    @foreach (var record in CinemaModel.FilmList)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => record.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => record.ShowList)
            </td>
        </tr>
        }
    </table>

}
